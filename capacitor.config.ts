import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mindspace.app',
  appName: 'mind-space',
  webDir: 'dist/MindSpace/browser',
};

export default config;
